---
# The live consumer shape that motivated the check: session_timeout has never
# been a parameter of the class, and Hiera ignored it without a whisper.
rootless_gitlab_runner::concurrent: 1
rootless_gitlab_runner::session_timeout: 1800
rootless_gitlab_runner::packages:
  install: []
rootless_gitlab_runner::runners: []
