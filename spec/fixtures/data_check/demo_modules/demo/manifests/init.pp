# Fixture class for the data-versus-surface check spec: a minimal declared
# surface with a struct-shaped parameter carrying a manage toggle.
class demo (
  Hash $standalone = {},
  Integer $alpha = 1,
) {
}
